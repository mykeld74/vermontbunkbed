import { collection } from './collection';
import { finish } from './finish';
import { productType } from './productType';
import { siteSettings } from './siteSettings';

export const schemaTypes = [collection, productType, finish, siteSettings];
